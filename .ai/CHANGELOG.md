# История итераций ИСУА

Хронологический журнал работы. Каноничный источник: сюда вносятся изменения в конце
каждой итерации. Активный бэклог — в `REQUIREMENTS_BACKLOG.md`; план следующей итерации —
в `NEXT_ITERATION.md`; состояние/архитектура — в `CONTEXT.md`.

Ветка `dev` (main = стабильный прод, Vercel автодеплоит main).

## Итерация 22 (перевод агрегаций на SQL groupBy/aggregate, 2026-07-16) — версия 0.22.0

Тема: агрегаты статистики переведены с загрузки строк в память на вычисление
на уровне БД (Prisma `groupBy`/`aggregate`). Цель — убрать
`program.findMany({ include: { applicants } })`, который тащил **все строки**
абитуриентов в Node-память; запросы теперь возвращают O(программ) строк вместо
O(абитуриентов). Память и трафик кратно меньше при росте данных.

### 1. Слои агрегации (разделены)
- **`src/lib/program-stats.ts`** — теперь **чистый мёрджер** без импорта Prisma.
  `aggregateProgramStats(programs, agg, options)` собирает `ProgramStatRow[]` из
  метаданных программ (`ProgramMeta`) + Map'ов БД-агрегатов (`ProgramAggregatesInput`).
  `groupProgramStats(byProgram, programs)` без изменений (тип аргумента → `ProgramMeta[]`).
  Прежний `ProgramWithApplicants` (с массивом applicants) и `aggregateProgramsToStats`
  удалены. Расчёт `topApplicants` убран (см. ниже).
- **`src/lib/program-stats-db.ts`** (новый, server-only) — оркестратор `loadProgramStats(
  prisma, options, includeGroups)`. Делает один `program.findMany` **без** `applicants`
  + `Promise.all` из 8 `applicant.groupBy`: count+avg, по статусам, по каждому флагу
  (consent/documents/paid/distant), пересечение distant×consent, опционально newToday.
  Каждый результат → в `Map<programId, …>`, потом вызов чистого мёрджера.

### 2. Эндпоинты переведены на БД-агрегаты
- **`/api/programs/stats`**: было **1** `findMany` (грузил все строки applicants) →
  стало 8 мелких запросов (1 findMany без applicants + 7 groupBy), каждый O(программ).
- **`/api/stats/daily`**: 2 `count` + 1 `history.findMany` без изменений; блок
  `program.findMany({include:{applicants}})` + мёрджер заменён на `loadProgramStats` с
  `dayWindow` (newToday) и `consentMovement` (нетто-согласия из History). Структура
  ответа `DailyStats` **без изменений** — обратная совместимость.

### 3. Удаление `topApplicants` (топ-3 по баллу на программу)
- Поле `topApplicants` из `ProgramStatRow` (`src/lib/api.ts`) убрано — единственная
  метрика, требовавшая строк (`fullName`), и ради неё грузился весь список. Без неё
  агрегаты — чистый `groupBy`/`aggregate` без выборки строк.
- В `src/components/programs-view.tsx` удалён блок «Топ-3 по баллам» + неиспользуемый
  импорт `Trophy`. Карточка программы: места, чипы статусов, средний балл, конкурс,
  согласия, кнопка «Все абитуриенты».

### 4. Тесты (152 → 160, +8)
- **`src/lib/program-stats.test.ts`** (новый, 9 кейсов): unit-тесты чистого мёрджера —
  ранее `program-stats.ts` покрывался только косвенно. Покрыты edge-cases: `places=0`
  (competition null, consentFillPercent 0), avgScore null при отсутствии в Map,
  newToday только из dayWindow-агрегата, consentMovement из options, программа без группы.
- `programs/stats/route.test.ts`: мок переключён с `include:{applicants}` на
  `program.findMany` (без applicants) + `applicant.groupBy` (параметризованный по
  аргументам запроса). Удалён тест `topApplicants`.
- `stats/daily/route.test.ts`: мок перестроен под БД-агрегаты (`applicant.groupBy`).

### Компромисс архитектуры
Больше round-trip'ов к БД (1 → 8 на `/programs`), но все параллельны через `Promise.all`
и каждый идёт по проиндексированному `@@index([programId])`. Выигрыш — в памяти и
трафике, не в числе запросов. Сырой SQL (`$queryRaw`) сознательно не вводился — стиль
«чистый Prisma»; при желании свернуть 8 groupBy в один `COUNT(*) FILTER` — в бэклог.

### Не сделано (границы)
- Без миграции БД / новых индексов (код-only, как итер. 21). Индексы на булевы флаги —
  в бэклог, если профиль покажет необходимость.
- Без min/max totalScore, без возврата topApplicants.

## Итерация 21 (оптимизация stats/daily + разделение /programs, 2026-07-16) — версия 0.21.0

Тема: расследование «медленного `GET /` на проде» → оптимизация тяжёлых API-запросов.

### Расследование бага `GET /` (21.3с, ERR_ABORTED)
- **Вывод: баг не кодовый.** `next build` подтверждает, что `/` помечен
  `○ (Static)` — отдаётся с edge CDN как статический HTML-шелл и НЕ попадает в
  serverless-функцию. 21.3с на статику + Hobby-лимит функции 10с → не мог быть
  таймаутом функции. Транзиентный edge/CDN-stall или сетевой сбой (Vercel).
- Корневой `page.tsx` — клиентский компонент; `layout.tsx` лёгкий (шрифты +
  metadata, без `cookies()`/`headers()`); `middleware.ts` отсутствует.
- Баг не воспроизводится сейчас; доступ к Vercel-логам отсутствует. Закрыто как
  платформенное, без кодового фикса.

### Реальная проблема (найдена в коде): жирные API-запросы
`/api/stats/daily` выполнял **10 запросов к БД** на один вызов: 7 дублирующих
`count` (consent/documents/paid/distant/distantWithConsent) + `groupBy` по
статусам + `history.findMany` + `program.findMany` с applicants. При этом
верхнеуровневые метрики дублировали данные, уже загруженные для `byProgram`.
Вкладка `/programs` дёргала тот же тяжёлый эндпоинт, хотя использовала только
`byProgram` (без history и верхнеуровневых метрик).

### 1. Переиспользуемая агрегация (`src/lib/program-stats.ts`, новый)
- `aggregateProgramsToStats(programs, opts)` — считает по каждой программе все
  метрики (applicants/competition/avgScore/withConsent/withDocuments/withPaid/
  withDistant/distantWithConsent/newToday/consentGivenToday/consentWithdrawnToday/
  consentFillPercent/applied/withdrawn/topApplicants) из уже загруженных
  applicants — без дополнительных запросов к БД.
- `groupProgramStats(byProgram, programs)` — группировка программ по группам с
  подытогами (вынесена из route.ts без изменений логики).
- Тип `ProgramWithApplicants` — минимальный набор полей для агрегации.

### 2. Рефакторинг `/api/stats/daily` (10 → 4 запроса)
- Убраны 7 дублирующих `count`-запросов + `groupBy` по статусам. Верхнеуровневые
  метрики (withConsent/withDocuments/withPaid/withDistant/distantWithConsent/
  applied/withdrawn) теперь суммируются из `byProgram` — те же данные, что и так
  загружались для программ.
- Осталось 4 параллельных запроса в `Promise.all`: `total` (count), `newToday`
  (count по createdAt), `consentChangesToday` (history), `programs` (с applicants).
- Структура ответа `DailyStats` **без изменений** — обратная совместимость.

### 3. Новый лёгкий эндпоинт `/api/programs/stats` (1 запрос)
- `GET /api/programs/stats` — только агрегация программ, без history и без
  верхнеуровневых метрик дашборда. Возвращает `{ byProgram: ProgramStatRow[] }`.
- Использует ту же `aggregateProgramsToStats` (без дневного окна → newToday и
  consentGivenToday/consentWithdrawnToday всегда 0 — вкладке не нужны).
- `programs-view.tsx` переключён с `statsApi.daily()` на `programsApi.stats()`.
  Удалены: импорт `useAppStore`/`timezone` (больше не нужен), тип `DailyStats`.

### 4. API-слой (`src/lib/api.ts`)
- `programsApi.stats()` — обёртка для нового эндпоинта.

### Тесты (147 → 152, +5)
- `stats/daily/route.test.ts`: моки переписаны под 4-запросную структуру (count
  только для total и newToday, groupBy убран). Добавлен кейс: верхнеуровневые
  метрики считаются из загруженных applicants, count не ищет по consent/documents.
- `programs/stats/route.test.ts` (новый, 5 кейсов): авторизация, структура
  byProgram с агрегатами, topApplicants, newToday=0, ровно 1 запрос findMany.

### Эффект
- `/` (дашборд): **10 → 4 запросов** к БД на вызов `/api/stats/daily` (-60%).
- `/programs`: **10 → 1 запрос** к БД (раньше полный daily, теперь только программы).
- Билд: `/` и `/programs` остаются `○ (Static)`; новые/изменённые эндпоинты `ƒ (Dynamic)`.

## Итерация 20 (чистка UI + ВИ по предметам, 2026-07-09) — версия 0.20.0

Тема: две задачи — удаление избыточного экрана «Статусы» и разбор ВИ по предметам.

### 1. Удаление вкладки «Статусы»
- [x] Экран `/statuses` (Kanban applied/withdrawn) удалён — маршрут `src/app/statuses/`
  и компонент `statuses-view.tsx`. Навигация (desktop + mobile tab bar) и неиспользуемый
  импорт `ListChecks` убраны из `header.tsx`. Функционал полностью покрыт фильтрами
  таблицы `/applicants` и карточкой абитуриента. Отдельных API/тестов для вкладки не было.

### 2. ВИ по предметам (A-bis, гибрид)
- [x] **Модель (`schema.prisma`):** enum `ExamType { ege vi }`; `Applicant.examType`
  (default `ege`); 6 колонок vi-предметов (`viMathProfile/viRussian/viChemistry/viPhysics/
  viInformatics/viGeography`, Int?, 0–100). Поле `viScore` (0–300) сохранено (общий балл).
  Миграция `add_exam_type_and_vi_subjects` (+ `UPDATE … SET examType='vi' WHERE viScore
  IS NOT NULL` — существующие ВИ-абитуриенты помечаются корректно).
- [x] **Расчёт (`scoring.ts`):** `VI_SCORE_FIELDS` (6) + `ExamType` + `EXAM_TYPES`.
  `calculateTotalScore(applicant, additionalScores, viScore, examType, viScores)`:
  `examType="vi"` → viScore (если задан) + доп; иначе топ-3 vi-предметов + доп (≥3 нужно);
  иначе null. `examType="ege"` → прежняя логика топ-3 ЕГЭ (ЕГЭ и ВИ взаимоисключающи).
- [x] **Валидация:** `examType` (enum) + 6 vi-предметов = `scoreField` (0–100).
- [x] **API:** POST/PUT принимают `examType`+vi-предметы; пересчёт total по новой
  сигнатуре; `SIMPLE_FIELDS`/`scoreTouched` расширены (vi-предметы, examType).
- [x] **Форма (главное):** переключатель «Вид экзамена: ЕГЭ/ВИ» в секции «Баллы». **При
  переключении данные НЕ стираются** — оба набора баллов хранятся в полях; взаимоисключение
  resolved при сохранении (payload обнуляет «чужой» набор), как и mathBase/mathProfile.
  Режим ВИ: общее поле «Балл ВИ» (0–300, необяз.) + сетка 6 vi-предметов; live-пересчёт.
- [x] **Таблица:** маркер ВИ (точка) теперь по `examType==="vi"` (3 места: строка,
  мобильная карточка, легенда). В строке-детали — vi-предметы + общий балл ВИ.
- [x] **Экспорт XLSX:** колонки «Вид экзамена» + 6 vi-предметов добавлены в **конец**
  (после ПДн) — НЕ сдвигают ссылочные D/E/H/I/M в формулах листа «Статистика». Колонка
  «ВИ» (F) — сводка.
- [x] **История/метки:** `examType` + vi-предметы в `LOGGED_FIELDS`; метки в `FIELD_LABELS`
  («ВИ: …»); `examType` в истории — «ЕГЭ»/«ВИ».
- [x] **Seed:** 15% vi-абитуриентов (`examType="vi"`, vi-предметы rndScore 0–100,
  ЕГЭ=null, viScore=null — тестит топ-3 vi-ветку).
- [x] **Тесты 143 → 147:** блок ВИ в `scoring.test.ts` переписан под гибрид (+4 кейса:
  vi+viScore, vi+топ-3 vi-предметов, vi приоритет viScore, ege игнор viScore; <3 vi → null).

## Патч 0.19.2 (ремонт PDF-отчёта: кнопка + визуал + ПДн, 2026-07-09)

### Починка
- [x] **Кнопка «Отчёт PDF» не реагировала:** `onClick={() => window.open("/report", "_blank")}`
  блокировалась попап-блокировщиком браузера → `null`, без эффекта. Фикс: нативная ссылка
  `<a href="/report" target="_blank" rel="noopener noreferrer">`, стилизованная как кнопка через
  новый `buttonClasses(variant, size)` в `ui.tsx`. Не блокируется по user-gesture + работает
  «Открыть в новой вкладке».

### Надёжность страницы отчёта (`/report`)
- [x] `load()` без `catch` → вечный спиннер при ошибке API. Добавлен `error`-state + `catch`
  (зеркало паттерна дашборда) + UI ошибки с кнопкой «Повторить».
- [x] `AuthGuard` обёрнут вокруг страницы (без `AppShell` — чистый print): при истёкшей сессии
  редирект на `/login`, а не зависший спиннер.

### Безопасность (ПДн)
- [x] **Убраны паспортные данные из отчёта:** колонка «Паспорт» удалена из рейтинга (ранее
  серия+номер печатались всем ролям, включая операторов). ПДн убраны только из рендера, не из API.

### Визуал отчёта → дизайн-система (раньше голые таблицы)
- [x] Общая статистика → сетка MetricItem (иконка-тайл + крупное значение), главная метрика
  акцентирована emerald (`border-emerald-300 bg-emerald-50`).
- [x] Каждая секция в `<Card className="overflow-hidden">` с хедер-полосой `border-b`.
- [x] Таблицы: header `bg-slate-50 text-slate-500 px-4 py-2`; конкуренция ≥1 → `text-emerald-700`.
- [x] Маркеры Б/ОК/ОП/П/Д → цветные бейджи (red/amber/slate/indigo) вместо plain text.
- [x] Согласие/документы → emerald `Check` / красный `✗` (раньше голый `✓`/пусто).
- [x] Контейнер `bg-slate-50`, белый фон только при печати (`print:bg-white`).

### Print-CSS (`globals.css`)
- [x] `@media print` rem поднят 12px → **14px** (масштаб ~115–120% без ручного ползунка).
- [x] `print-color-adjust: exact` — иначе браузер убирает фоновые цвета бейджей в PDF.

### Прочее
- `src/components/ui.tsx`: `buttonClasses(variant, size, className?)` — переиспользование стилей
  кнопки на `<a>`.
- `.gitignore`: игнор `.ai/MICROSERVICES.md`, `.ai/MODULAR_MONOLITH.md` (предсуществующее изменение).

## Патч 0.19.1 (баг-фиксы + метрика «дистант × согласие», 2026-07-09)

### Баг-фиксы (регрессии итерации 19 / патча 0.18.3)
- [x] **Чипы-фильтры не работали:** `toQuery` сериализовал `true` как строку `"true"`,
  а бэк строго проверял `=== "1"` → параметр игнорировался. Фикс: `true` → `"1"`,
  `false`/`undefined` отбрасываются. Тест на сериализацию boolean-чипов в query.
- [x] **«Все» показывало 50, а не все:** `Number(sp.get("limit")) || 50` — `Number("0")=0`,
  а `0 || 50 = 50` (0 falsy). Бэк молча подменял «Все» на 50. Фикс: различать отсутствие
  параметра и `"0"` (`limitParam === null ? 50 : Number(limitParam)`). +2 теста (limit=0 →
  skip/take undefined; без limit → дефолт 50).
- [x] **Пагинация «Все» (Infinity / «1–0 из N»):** `Math.ceil(total/0)=Infinity` →
  «Стр. 1 из Infinity», кнопки навигации активны. Фикс: режим `allMode` при `limit=0` →
  `totalPages=1`, `to=total`, блок навигации скрыт.

### Метрика «Дистант × согласие»
- [x] **Пересечение `isDistant × consentToEnroll`** (которого не было — счётчики считались
  независимо). Новое поле `distantWithConsent` в `stats/daily`: верхний уровень
  (отдельный count), `byProgram` (filter), `byGroup` subtotal (reduce). «Без согласия» =
  `withDistant − distantWithConsent` (на клиенте).
- [x] **Дашборд:** карточка «Дистанционно» стала составной — крупно всего + `% от всех`,
  под ним две строки (● с согл. / ● без согл.) с цветными точками. В таблице «Конкурс по
  программам» колонка «Дистант» показывает сноску «с_согл/всего» (строка, подытог, итог).
- [x] **XLSX-экспорт:** на листе «Статистика» новый блок «Дистант» (всего / с согласием /
  без / %) через `COUNTIFS` по колонкам Дистант (M) × Согласие (H); в разбивке по
  программам — колонка G «Дистант с согл.» (3 условия COUNTIFS). Лист данных не тронут.
- [x] **Тесты 136 → 143:** новый `stats/daily/route.test.ts` (5 кейсов: структура ответа,
  byProgram, byGroup subtotal, пересечение where). Typecheck/ESLint 0 ошибок.

## Итерация 19 (UX таблицы абитуриентов, 2026-07-09) — версия 0.19.0

Тема: три продуктовых улучшения — порядок сортировки, быстрый отбор по маркерам и
сброс пароля администратором. Баг прод-маршрута `/` в итерации не затрагивался.

- [x] **Сортировка `NULLS LAST` (Пакет 1):** при сортировке по `totalScore` (DESC)
  прочерки (null) больше не лезут в начало списка — null-значения уходят в конец.
  `GET /api/applicants` (`route.ts`) для nullable-полей использует расширенную форму
  Prisma `{ field: { sort, nulls: "last" } }` (множество `NULLABLE_SORT = {totalScore}`);
  добавлен стабильный тай-брейк по `createdAt` (массив `orderBy`).
- [x] **Мульти-фильтры — чипы-тогглы (Пакет 2):** в панели инструментов таблицы
  добавлен ряд toggle-кнопок по маркерам: **ОК** (особая квота), **ОП** (особое право),
  **Согл.** (согласие на зачисление), **Док.** (документы готовы), **Платн.**, **Дистант**.
  Несколько активных комбинируются через AND. Реализовано по паттерну кнопок «По дням»
  (variant primary/secondary) — без новых UI-примитивов. Бэк: query-параметры
  `?ok=1&op=1&consent=1&docs=1&paid=1&distant=1` → `where` (AND). Тип `ApplicantFilters`
  расширен 6 boolean-полями; `toQuery` отбрасывает `false` (неактивные чипы не уходят в URL).
- [x] **Сброс пароля администратором (Пакет 3):** новый эндпоинт
  `POST /api/users/[id]/reset-password` (только admin) — хеширует новый пароль и
  инкрементит `tokenVersion` целевого юзера, отзывая все его сессии (cookie админа не
  трогается). Метод `usersApi.resetPassword`; в `UserManager` — кнопка-иконка (KeyRound)
  и модалка сброса. Новая Zod-схема `adminResetPasswordSchema` (min 8, без проверки
  «текущего»). Для своего пароля остаётся смена в шапке (итер. 14).
- [x] **Тесты 101 → 135:** +3 кейса сортировки (NULLS LAST, тай-брейк), +3 кейса
  чипов-фильтров (AND, игнор не-`1`), +5 кейсов reset-password. Typecheck/ESLint 0 ошибок.

## Патч 0.18.3 (опция «Все» в таблице абитуриентов, 2026-07-04)

- [x] **Опция «Все» в выборе размера страницы:** в выпадающем списке таблицы добавлен
  вариант «Все» (value=0) — отображает все записи без пагинации. Серверный эндпоинт
  `/api/applicants` при `limit=0` убирает `skip`/`take` и возвращает полный результат.

---

## Патч 0.18.2 (нормализация регистра ФИО, 2026-06-30)

- [x] **Нормализация ФИО (`name-case.ts`):** утилита `normalizeName` приводит `fullName`
  к стандартному регистру — первая буква заглавная, остальные строчные. Учитывает
  патронимические суффиксы (оглы, кызы, улы, сен, торе, заде — остаются строчными),
  дефисные имена (Иванов-Петров) и апострофы (д'Артаньян). 23 теста.
- [x] **Интеграция в Zod-схему:** поле `fullName` в `validation.ts` проходит
  `.trim().transform(normalizeName)` — нормализация автоматически применяется
  при создании и обновлении абитуриента (серверная точка).
- [x] **One-time скрипт `scripts/normalize-names.ts`:** проход по всем существующим
  записям Applicant, нормализация fullName, обновление в БД + запись в History.
  Поддерживает dry-run (`NORMALIZE_NAMES_DRY=1`).

---

## Патч 0.18.1 (легенда обозначений + цветовая полировка, 2026-06-26)

- [x] **Легенда обозначений:** кнопка «Обозначения» в панели инструментов таблицы раскрывает
  блок-легенду (`Legend`) — перечень всех маркеров и иконок (Б/ОК/ОП/П/Д/З, ВИ-точка,
  предупреждение, галочки Согл./Dok.) с пояснениями. Иконки в легенде синхронизированы
  со стилями компонента `<Markers>`.
- [x] **Цветовая полировка маркеров:** Б (`rose-100→red-50/red-600`), ОК/ОП объединены
  в единый `amber-50/amber-700`, П (`slate-200→slate-100/slate-500`), Д (`sky-100→indigo-50/indigo-600`).
  Крестики «не дал согласие / документы не готовы» из `rose-400` в `red-400`.
- [x] **ВИ-маркер:** бейдж «ВИ» заменён компактной точкой (`inline-block size-2 rounded-full bg-amber-400`)
  — одинаково в таблице и мобильной карточке.
- [x] **README актуализирован:** добавлены `ENCRYPTION_KEY_OLD`, `SEED_DATA`, скрипт
  `crypto:rotate`; исправлено устаревшее предупреждение о потере ПДн при смене ключа.
- [x] **CONTEXT.md:** исправлена таблица документов (CHANGELOG 1–14 → 1–18, кандидаты 15 → 19).

---

## Итерация 18 (дата рождения + подытоги конкурса, 2026-06-25) — версия 0.18.0

Тема: два пользовательских улучшения — **дата рождения абитуриента** (новое поле)
и **подытоги/итог в таблице конкурса по программам** на дашборде.

- [x] **Дата рождения — модель:** новое поле `Applicant.birthDate DateTime? @db.Date`
  (только дата, без времени; миграция `20260625154118_add_birth_date`). Не шифруется
  (как `citizenship`), nullable.
- [x] **Валидация (`validation.ts`):** поле `birthDate` — пусто/null → null; иначе
  валидная дата с проверкой диапазона (год ≥ 1900 и не в будущем). HTML-инпут
  `type="date"` отдаёт `YYYY-MM-DD`, `z.coerce.date` парсит как UTC-полночь.
- [x] **API:** POST/PUT `/api/applicants` принимают `birthDate`; логируется в истории
  (`history.ts` + `toStr` форматирует `Date` как `YYYY-MM-DD` для стабильного сравнения).
- [x] **Форма абитуриента:** поле «Дата рождения» (`type="date"`, `max=сегодня`) в секции
  «Персональное».
- [x] **Таблица/карточки:** «Дата рождения» в раскрытой строке-детали и мобильной карточке
  (`formatBirthDate` форматирует в `dd.MM.yyyy` по UTC, чтобы зона браузера не сдвигала день).
- [x] **Экспорт XLSX:** колонка «Дата рождения» добавлена ПОСЛЕ ссылочных столбцов
  (C/D/E/H/I), чтобы не сдвигать буквы в формулах листа «Статистика».
- [x] **История:** новая подпись поля `birthDate` → «Дата рождения»; значение в истории
  форматируется в `dd.MM.yyyy`.
- [x] **Подытоги конкурса (дашборд):** строка-заголовок группы в таблице «Конкурс по
  программам» теперь показывает подытог по группе (места, абитуриенты, новые сегодня,
  конкурс, согласия, документы, платные, дистант). API (`stats/daily`) расширил
  `subtotal` полями `withDocuments/withPaid/withDistant` + `competition`.
- [x] **Итоговая строка:** в `tfoot` таблицы конкурса добавлена строка «Итого» по всем
  программам (места, абитуриенты, новые, конкурс, согласия, документы, платные, дистант).
- [x] **Тесты 94→101:** +5 кейсов валидации `birthDate`, +2 кейса истории `Date→YYYY-MM-DD`.

## Итерация 17 (разделение «Доп. баллы / ВИ», 2026-06-25) — версия 0.17.0

Тема: разнести единое поле «Доп. баллы / ВИ» на два независимых — **доп. баллы**
(индивидуальные достижения, 0–10) и **ВИ** (вступительные испытания вуза, 0–300).

- [x] **Модель данных:** новое поле `Applicant.viScore Int?` (миграция
  `20260625120000_add_vi_score`). Существующие `additionalScores` не трогаются —
  остаются доп. баллами (миграция данных не требуется).
- [x] **Расчёт балла (`scoring.ts`):** `calculateTotalScore` получил 3-й аргумент
  `viScore`. Если ВИ задано — `total = ВИ + доп. баллы` (предметы ЕГЭ **игнорируются**,
  ВИ заменяет сумму ЕГЭ); иначе — прежняя логика (топ-3 ЕГЭ + доп. баллы, нужно ≥3
  предметов). Константы `MAX_ADDITIONAL_SCORES=10`, `MAX_VI_SCORE=300`.
- [x] **Валидация (`validation.ts`):** cap доп. баллов 100 → **10**; новое поле
  `viScore` (целое 0–300 либо null). Логируется в истории (`history.ts`).
- [x] **Форма абитуриента:** отдельные поля «Доп. баллы» (0–10) и «ВИ» (0–300);
  live-пересчёт учитывает ВИ; подпись итога меняется на «ВИ + доп. баллы (предметы
  ЕГЭ не учитываются)», когда ВИ заполнено.
- [x] **Таблица/карточки:** у балла ВИ-абитуриентов — голубой бейдж **«ВИ»**; в деталях
  строки показываются ВИ и доп. баллы.
- [x] **Экспорт XLSX:** добавлены столбцы «ВИ» и «Доп. баллы» (формулы статистики
  пересчитаны под сдвиг столбцов).
- [x] **API:** POST/PUT `/api/applicants` принимают и пересчитывают `viScore`; seed
  генерирует ~15% ВИ-абитуриентов. Тесты 89→94 (+5 кейсов ВИ в `scoring.test.ts`).

## Итерация 16 (ротация ключа шифрования — M3, 2026-06-25) — версия 0.16.0

Тема: безопасная смена `ENCRYPTION_KEY` без потери данных (закрывает M3 из `SECURITY.md`).

- [x] **Keyring в `lib/crypto.ts`:** `encrypt` всегда шифрует текущим `ENCRYPTION_KEY`;
  `decrypt` пробует текущий ключ, затем старые из `ENCRYPTION_KEY_OLD` (через запятую) —
  auth-tag GCM однозначно выявляет верный. Формат остаётся `enc:v1`. Новые помощники:
  `isUnderCurrentKey` (идемпотентность ротации), `resetKeyCache` (сброс кеша ключей).
- [x] **Скрипт `npm run crypto:rotate`** (`prisma/rotate-encryption.ts`): проходит абитуриентов
  батчами (keyset по id), для каждого PII-поля decrypt (keyring) → encrypt (новый ключ) → update.
  Идемпотентно (поля под текущим ключом пропускаются), безопасно (нерасшифрованное не трогается),
  с логом счётчиков. Флаг `-- --dry-run` — предпросмотр без записи.
- [x] **`.env.example`:** задокументирован `ENCRYPTION_KEY_OLD`; убрано устаревшее предупреждение
  «смена ключа → данные нечитаемы».
- [x] **Runbook ротации** — в `SECURITY.md` (раздел «Ротация ключа шифрования»). M3 → закрыто.
- [x] **Тесты `crypto.test.ts`:** keyring decrypt (новый+старый), ре-шифрование (читаемо только
  новым ключом), неверный ключ → null, список старых ключей, `isUnderCurrentKey`. Тесты 84→89.

## Патч 0.15.3 (создатель записи в истории, 2026-06-23)

- [x] В истории абитуриента появилось событие **«Создание записи»** (самое старое)
  с именем создателя и датой. Берётся из `createdByUserId`/`createdAt` самого
  абитуриента (синтетически в GET `/history`) — работает и для записей, заведённых
  раньше, без миграции. Записи без правок показывают «Запись создана» вместо
  «Изменений пока нет». Тесты 81→84.

## Патч 0.15.2 (фикс «сегодня» по таймзоне, 2026-06-23)

- [x] «Сегодня» в `/api/stats/daily` теперь считается по **локальной дате таймзоны**
  (`todayInTimeZone`), а не по UTC-дате. Раньше возле полуночи в зонах ≠ UTC счётчик
  новых заявлений/согласий отставал на сутки (напр. до ~03:00 МСК показывал прошлые
  сутки). Тесты 79→81.

## Патч 0.15.1 (UX дашборда)

- [x] Изменён порядок карточек статистики на дашборде; защита refetch 15с → 45с.

---

## Итерация 15 (группировка программ + статистика, 2026-06-22) — версия 0.15.0

Тема: группировка программ по категориям + доработка статистики и интерфейса.

- [x] **Модель `ProgramGroup`** (id, name uniq, sortOrder) + `Program.programGroupId`
  (FK, `onDelete: SetNull` — удаление группы не удаляет программы). Миграция
  `add_program_group`. Сид: демо-группы «Инженерные»/«Землеустройство» (под `SEED_DATA=1`).
- [x] **API групп:** `GET/POST /api/program-groups`, `PUT/DELETE /api/program-groups/[id]`
  (запись — admin-only). `programs` отдаёт `groupId/groupName`, принимает `programGroupId`
  (connect/disconnect). Валидация `programGroupSchema` + поле `programGroupId`.
- [x] **Статистика `/api/stats/daily`:** нетто-согласия за день в разрезе программы
  (`consentGivenToday`/`consentWithdrawnToday`), новый блок `byGroup` — группы с
  подытогами (places/applicants/withConsent/newToday/согласия сегодня).
- [x] **/manage:** блок «Группы программ» (CRUD: создать/переименовать/порядок/удалить) +
  столбец «Группа» с выпадающим списком у каждой программы.
- [x] **Дашборд:** карточки «Новые заявления сегодня» и «Новые согласия сегодня» рядом,
  сгруппированы с подытогами; «Согласия / места» сохранена; большая таблица «Конкурс»
  получила заголовки групп.
- [x] **Отчёт + XLSX:** группировка в таблице «Конкурс по программам»; столбец «Группа»
  в листе «Статистика» экспорта.
- Тесты 71→79 (program-groups API + валидация `programGroupId`). tsc + lint(0) чисто.

---

## Патч 0.14.1 (флаг «дистант», 2026-06-20)

Точечная доработка поверх итерации 14: добавлен флаг дистанционного обучения.

- [x] **Поле `Applicant.isDistant`** (Boolean, дефолт `false`) — миграция
  `20260620000000_add_is_distant`. Дефолт безопасен для существующих БД.
- [x] Сквозная проводка: Zod-валидация (`isDistant`), POST/PUT API, лог в `History`,
  сид (`SEED_DATA`), чекбокс «Дистант» в форме абитуриента, маркер в таблице и отчёте.
- [x] Статистика: счётчик `withDistant` в `/api/stats/daily` (общий и по программам),
  карточка «Дистанционно» на дашборде, бейдж «Дистант» на карточке программы,
  колонка «Дистант» в XLSX-экспорте.
- ⚠️ Миграцию накатить на каждую БД (`migrate deploy`) — см. `ops/DATABASE_ENVIRONMENTS.md`.

---

## Итерация 14 (учётки + экспорт, 2026-06-18) — версия 0.14.0

Тема: пункты D (учётные записи) и C (экспорт) из плана.

- [x] **Email регистронезависимый (D1):** нормализация `trim+lower` на уровне Zod-схемы
  (`emailField`), применена в register и login → поиск/уникальность по email не зависят
  от регистра. Для существующих БД — SQL-нормализация в `ops/DATABASE_ENVIRONMENTS.md`.
  Тесты валидации (4).
- [x] **Смена своего пароля (D2):** `POST /api/auth/change-password` (auth) — проверка
  текущего пароля, запрет совпадения нового со старым, **инкремент `tokenVersion`**
  (отзыв старых JWT, H2) + перевыдача cookie текущей сессии. Модалка `ChangePasswordModal`
  (текущий+новый+подтверждение), триггеры в шапке (десктоп иконка + «Ещё» на мобильном).
  Интеграционные тесты роута (5).
- [x] **Экспорт абитуриентов в XLSX (C):** `GET /api/applicants/export` (exceljs). Лист
  «Абитуриенты» (весь список, без фильтров) + лист «Статистика» со **встроенными формулами**
  (COUNTIF по статусам, AVERAGE/MIN/MAX баллов, % согласий/документов, разбивка по программам
  с конкурсом и AVERAGEIF). ПДн (паспорт/ИНН/СНИЛС/прописка) — колонки только для админа,
  расшифрованные; оператор получает файл без ПДн. Кнопка «Экспорт» на странице абитуриентов.
- Проверено E2E (bootstrap-admin → программа → 2 абитуриента → export): файл валиден, 2 листа,
  формулы корректны, ПДн-колонки у админа. БД возвращена в пустое состояние.
- Тесты 62→71. build + lint(0) чисто. Добавлена зависимость `exceljs`.

---

## Итерация 13 (полировка, 2026-06-18) — версия 0.13.0

Тема: накопленные точечные улучшения после ревью. Версия 0.13.0.

- [x] **Сидинг отключён по умолчанию:** `seed.ts` ничего не создаёт; данные заводятся
  вручную через интерфейс. Включить тестовые данные — флаг `SEED_DATA=1` (4 программы +
  демо-админ + 60 абитуриентов). Локальная БД обнулена. `.env.example`: `SEED_DATA` задокументирован.
- [x] **Squash миграций:** 8 файлов миграций схлопнуты в одну `20260618030000_init`
  (сгенерирована из `schema.prisma` через `migrate diff --from-empty`). Безопасно — БД
  накатана только локально на пустой БД; прод/dev-preview ещё нет. Предупреждение про
  squash при наличии данных — в `ops/DATABASE_ENVIRONMENTS.md`.
- [x] **Балльные поля Float → Int:** mathProfile/russian/chemistry/physics/informatics/
  geography/additionalScores/totalScore. Float был артефактом старой логики
  (totalScore=среднее); сейчас балл — сумма целых, валидация требует `.int()`. Честный тип,
  без float-погрешностей.
- [x] **Автоопределение таймзоны:** `detectTimezone()` через Intl (фолбэк Europe/Moscow на SSR);
  store.timezoneManual — applyAutoTimezone не перезатирает ручной выбор; app-shell эффект.
  Раньше зона была захардкодена. Тесты 55→62.
- build + lint(0 проблем) + 62 теста чисто.

---

## Итерация 12 (инфра БД + надёжность, 2026-06-18) — версия 0.12.0

Тема: 12A (ICU-локаль) + 12C (полировка надёжности).

- [x] **12A ICU-локаль БД:** локальная БД пересоздана с `LOCALE_PROVIDER icu` /
  `ICU_LOCALE 'ru-RU'` (dump → новая БД → restore → swap; старая C-locale сохранена как
  `isua_c_backup`). COLLATE-костыль в applicants GET убран → обычный Prisma OR + contains +
  `mode:"insensitive"`. Поиск по кириллице регистронезависим из коробки.
  `ops/DATABASE_ENVIRONMENTS.md` дополнен инструкцией для прод/dev.
- [x] **12C retry/переподключение:** `lib/api.ts` — до 3 попыток с backoff (300/900мс) для
  GET/HEAD при сетевой ошибке и 502/503/504; POST/PUT/DELETE НЕ повторяются; сетевой сбой →
  `ApiError status 0`.
- [x] **12C интеграционные тесты:** `/api/applicants` GET (auth/where/поиск/пагинация, 8 тестов),
  `/api/programs` POST (admin/валидация/дубликат, 4 теста), retry-обёртка (7 тестов). Всего 55
  (было 36).
- [x] **12C README:** заполнен (стек, запуск, ICU-БД, env, скрипты, тесты, деплой) вместо boilerplate.
- build + lint(0 проблем) чисто.

---

## Итерация 11 (мобильный интерфейс, 2026-06-17) — версии 0.11.0 + патч 0.11.1

Тема: mobile-first адаптив (один layout). Закрыт 7E.

- [x] Нижняя панель-таб-бар (4 иконки + «Ещё») на `<lg`; верхняя навигация на `≥lg`
  (граница `md→lg`).
- [x] Карточный режим таблицы абитуриентов на `<lg` (общие Markers/ApplicantDetails); убран гор. скролл.
- [x] Адаптив форм/модалок/сеток; Kanban стекается вертикально.
- [x] **Патч 0.11.1:** CSP `'unsafe-eval'` только в dev; фикс зазора header/main и перекрытия nav;
  дата в карточке на уровне «История»; иконка-кнопка удаления в /manage; heartbeat стоп при 401.
- Проверено Playwright 375/768/1280 — гор. скролла нет.

---

## Итерация 10 (безопасность H/M/L, 2026-06-17) — ветка dev

Тема: высокие и средние находки security-аудита. Закрыты H1, H2, H3, M2, M5, L4.

- [x] **H3 security-заголовки/CSP:** `next.config.ts` `headers()` — CSP (умеренный), HSTS,
  `X-Content-Type-Options=nosniff`, `X-Frame-Options=DENY`, Referrer-Policy, Permissions-Policy.
  CSP без nonce (`'unsafe-inline'` для Next inline) — не ломает рендер (0 CSP-ошибок).
- [x] **H2 отзыв токена:** `User.tokenVersion` (миграция add_token_version), claim `ver` в JWT,
  сверка в `getCurrentUser`, logout инкрементит → отзыв всех токенов. Старый токен после logout = 401.
- [x] **H1 trust-proxy:** `getClientIp()` — `x-vercel-forwarded-for` (Vercel), либо XFF при
  `TRUST_PROXY=1`, либо `x-real-ip`. Применён в login/register. Защита rate-limit от подделки IP.
- [x] **M2** SameSite=Strict для auth-cookie. **M5** cap `additionalScores ≤ 100`.
- [x] **L4** демо-админ не сеется на проде без `SEED_ADMIN_PASSWORD`. `.env.example`: `TRUST_PROXY`,
  `SEED_ADMIN_PASSWORD` задокументированы.
- E2E-проверено (fetch+Playwright): все заголовки, отзыв токена, SameSite=Strict, cap M5, рендер с CSP.

Подробности по находкам аудита — в `SECURITY.md`.

---

## Итерация 9 (полировка + инфра, 2026-06-17) — ветка dev

Тема: Приоритет 2 (полировка) + техдолг + инфраструктура сред.

- [x] **Тесты (unit, vitest):** настроен vitest (`vitest.config.ts`, `npm test`). 36 тестов:
  scoring (`calculateTotalScore`), applicant-logic (`normalizeConsent`), crypto (round-trip/
  идемпотентность/legacy/null), history (`buildHistoryEntries`), timezone (границы суток).
- [x] **Таймзоны:** `lib/timezone.ts` (`tzOffsetMinutes`/`dayBoundsUTC` через Intl) — корректные
  границы суток для ЛЮБОЙ зоны. Убран хардкод МСК=UTC+3/прочие=UTC из stats/daily.
- [x] **Toast вместо alert/confirm:** `lib/toast.ts` (Zustand) + Toaster (правый нижний угол);
  `lib/confirm.ts` + ConfirmDialog (модал подтверждения вместо `window.confirm`). Все 3 confirm
  заменены (program/bulk-delete/user manager); toast на успех/ошибку + сохранение абитуриента.
- [x] **Техдолг RHF:** `watch()` → `useWatch` в applicant-form-modal — ушёл ESLint-warning
  `incompatible-library`. Теперь ESLint по проекту = 0 проблем.
- [x] **Инфраструктура сред:** `DATABASE_ENVIRONMENTS.md` (→ `ops/`) — порядок работы с БД
  main/prod ↔ dev/preview (разные `DATABASE_URL` по средам Vercel; migrate deploy вручную).
- E2E-проверено (Playwright): toast при сохранении, ConfirmDialog (не нативный confirm).

---

## Итерация 8 (безопасность, 2026-06-17) — версия 0.8.x

Тема: контроль доступа и закрытие регистрации (закрывает A, C, K1, K2; B задокументирован).

- [x] **Роли admin/operator** (C, K1): enum `Role` в схеме (миграция add_user_role, существующие
  юзеры → admin). `role` в JWT и AuthUser; роль берётся из БД в `getCurrentUser` (смена применяется
  сразу). Хелпер `requireAdmin`, `forbidden()` (403).
- [x] **Серверные ограничения** (K1): admin-only на POST/PUT/DELETE programs, bulk-delete, удаление
  абитуриента, `/api/users`. operator может CRUD абитуриентов, кроме удаления.
- [x] **Закрытие регистрации** (A, K2): публичный `/register` закрыт — создавать юзеров может только
  admin; bootstrap первого admin на пустой БД (под rate-limit 5/15мин по IP); 409 — единое
  сообщение (анти-enumeration). Ссылка «Регистрация» убрана с экрана логина.
- [x] **UI ролей:** «Управление» скрыто от operator + серверный/клиентский гард /manage; бейдж роли
  в шапке; новый компонент UserManager (список юзеров, создание, смена роли, удаление).
  API `/api/users` (GET) и `/api/users/[id]` (PATCH role, DELETE) — admin-only.
- [x] **Защиты целостности:** нельзя удалить себя, снять/удалить последнего admin, удалить юзера с
  созданными им абитуриентами.
- [x] **B (документация секретов):** `.env.example` дополнен предупреждениями (прод-настройка Vercel:
  переменные в Settings не файлом; Redeploy; смена ENCRYPTION_KEY = потеря ПДн).
- [x] seed: демо-admin явно с ролью admin (иначе на новой БД был бы operator).
- **E2E-проверено:** operator получает 403 на delete/programs/users/bulk-delete, 200/201 на CRUD
  абитуриентов; admin — всё; нельзя удалить себя (409).

### Особое право (0.8.1)
- [x] Поле `specialRight`, чекбокс, маркеры ОК/ОП (две градации жёлтого).

---

## Итерация 7 (7A–7G, 2026-06-13)

- [x] **7A. Дашборд: «движение согласий за день» ОБНОВЛЯЕТСЯ, а не накапливается.** В `stats/daily`
  согласия за день считаются НЕТТО — по каждому абитуриенту сравнивается состояние на начало дня
  (oldValue первого изменения) с состоянием на конец дня (newValue последнего изменения);
  туда-сюда за день → 0. Где: `src/app/api/stats/daily/route.ts`.
- [x] **7B. Карточка абитуриента: вкладки → секции одна под другой.** В applicant-form-modal убраны
  TABS, добавлен компонент Section (заголовок-разделитель), все 5 секций идут вертикально, модал
  скроллится оверлеем. История остаётся кнопкой-часами в таблице.
- [x] **7C. Шифрование личных данных в БД (обратимое).** AES-256-GCM (`node:crypto`) в `lib/crypto.ts`;
  формат `enc:v1:iv:tag:ct` (base64), ключ `ENCRYPTION_KEY` (32 байта hex). Шифруются
  passportSeries, passportNumber, inn, snils (без телефона/email — поиск не нужен → blind-index не
  понадобился). `lib/applicant-pii.ts` (encryptPii/decryptPii): шифрование на границе БД. POST/PUT/
  GET/список/report: пишут зашифрованным, отдают расшифрованным. История: ПДн логируются МАСКОЙ «•••».
  Миграция существующих данных: `prisma/encrypt-pii.ts` (идемпотентно).
- [x] **7D. Таблица: раскрывающаяся строка-деталь.** Сокращена с 13 до 9 колонок (убраны
  Гражданство/Паспорт/Телефон/Email); добавлена колонка-шеврон. Клик по шеврону раскрывает под-строку
  (colSpan) с деталями + заметка целиком. У кого есть заметка — жёлтая иконка StickyNote у ФИО.
  min-w 58→44rem. Доработка (0.7.3): ФИО без fade, колонка w-auto+truncate; возвращена колонка
  «Телефон» (min-w 44→50rem). Доработка (0.7.4): в строку-деталь добавлены СНИЛС/ИНН/Прописка/Документ.
- [x] **7F. Авто-обновление таблицы (polling 20с).** В applicant-table тихий polling — setInterval
  каждые 20 сек вызывает `load(true)` (silent: без спиннера, без сброса строк при ошибке).
  load зависит от filters → фоновый refetch уважает текущие фильтры/поиск/страницу.
- [x] **7G. История: название программы вместо ID.** `formatHistoryValue` получил опц. параметр
  programNames (Map id→name); history-modal резолвит programId→название, фолбэк «Программа #id».

### Безопасность (аудит 2026-06-14, вместе с 7C)
- [x] **D:** токен убран из тела ответа login/register (остаётся только httpOnly-cookie).
- [x] **E:** rate-limit на login (`lib/rate-limit.ts`, in-memory, 10 попыток/15мин по IP+email → 429).

---

## Итерация 6 (таблица, 2026-06-13) — коммит 296cb4b

Итерация возникла из устных правок (не из backlog-пунктов).

- [x] Таблица: вернули фикс-ширину ФИО (w-72) после эксперимента с резиновой.
- [x] Таблица: фикс-шапка (sticky top-0), внутренний верт.+гориз. скролл, клик по строке = редактирование.
- [x] Колонка-распорка (w-auto) держит кнопку «История» на месте при сжатии окна.
- [x] Вкладка «Статусы»: убрана кнопка-карандаш (редактирование кликом по карточке).

---

## Итерация 5 (пороги, PDF, блокировки)

- [x] Минимальный порог баллов по программам (per-program minScores в Program; редактор в «Управлении»;
  предупреждения в форме/таблице).
- [x] PDF-отчёт: клиентский print (`/report`) — статистика + рейтинг с паспортом/платными/О/П + print-CSS.
- [x] Блокировки записей: REST-локи + heartbeat 10с + таймаут 30с (`useLock`, баннер «Редактирует …»).
- [x] Вкладка «Программы» (карточки с аналитикой). Вкладка «Статусы» (Kanban, 2 колонки applied/withdrawn).
- [x] Управление: /manage (program-manager + bulk-delete + пороги).
- [x] Исправлен баг Zod coerce (пустые баллы сохранялись как 0) — `z.preprocess(emptyToNull)`.

---

## Итерация 4 (история, метрики, группировка)

- [x] История-модал (кнопка-часы в таблице; человекочитаемые метки/значения).
- [x] Новые метрики дашборда: согласия за день (из History), платные, ratio заявл/места и заявл/согласия.
- [x] Таблица: группировка по дням (кнопка «По дням», свёртываемые разделители).
- [x] Удаление: отдельный интерфейс — вкладка «Управление», мультиселект + bulk-delete.

---

## Итерация 3 (поля, карточка, маркеры)

- [x] Поля абитуриента: паспорт, гражданство, тип документа, особая квота, платное
  (миграция add_passport_quota_paid_additional + re-seed).
- [x] Карточка: 5 вкладок (переупорядочение, без История).
- [x] Таблица: новые столбцы (паспорт, гражданство, дата/время 2 строки).
- [x] Маркеры Б/О/П после ФИО (Б — math база, О — квота, П — платно).
- [x] Математика: база и профиль взаимоисключающие (disabled в форме + обнуление на бэке; чинит баг балла).
- [x] Favicon (`src/app/icon.svg`, emerald шапочка). Wheel на баллах → blur.

---

## Итерация 1 (backend)

- [x] Backend, авторизация, дашборд, таблица абитуриентов.
- [x] Prisma-схема, seed (8 программ, 80 абитуриентов — эпоха SQLite, позже заменено на PostgreSQL + 5 моделей).
