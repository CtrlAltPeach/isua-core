# ИСУА — План следующей итерации

Обновлено: 2026-06-30. Работа в ветке `dev` (main = стабильный прод).
Закрыты итерации 1–18, патчи 0.18.1–0.18.2 (детали — CHANGELOG.md, SECURITY.md). Текущая версия 0.18.2.

Итерация 18 завершена (дата рождения + подытоги конкурса): новое поле
`Applicant.birthDate` (DATE, миграция `add_birth_date`) — форма/деталь/карточка/экспорт/
история; в таблице «Конкурс по программам» на дашборде — строка подытога по группе
(места/абит/новые/конкурс/согласия/документы/платные/дистант) и `tfoot` «Итого».
`stats/daily` subtotal расширен. Тесты 101.

Тема итерации 19 не выбрана — ниже кандидаты из остаточного бэклога.

---

## Кандидаты на итерацию 19 (выбрать тему)

### A. Функциональные доработки
- **Сортировка по баллам** — при сортировке по убыванию сейчас в начале списка идут прочерки
  (null-значения); должны сначала показываться те, у кого балл рассчитан и видим. Небольшая,
  заметная пользователю правка (whitelist сортировки `SORTABLE` + SQL `NULLS LAST` в
  `applicants/route.ts`).
- **Фильтры и сортировка в таблице абитуриентов** — добавить возможность отсеивать по статусам:
  с ОК (особая квота), с ОП (особое право), с согласием на зачисление, с документами.
  Продумать удобный UI для мульти-фильтрации (чекбоксы/переключатели/пег-бар) и комбинацию с
  существующей сортировкой. Цель — быстрый доступ к подгруппам: «кто принёс оригинал», «кто
  подал согласие» и т.д.
- **Сброс пароля пользователя админом** — в итерации 14 сделали только смену СВОЕГО пароля;
  сброс админом чужого пароля (через UserManager) отложен. Эндпоинт admin-only + инкремент
  tokenVersion целевого юзера.
- Экспорт CSV (без формул, для импорта в другие системы) — если понадобится в дополнение к XLSX.
- **Сортировка/перенос программ внутри группы** (drag-and-drop), если понадобится более тонкий
  порядок, чем `sortOrder` групп.

### A-bis. ВИ (вступительные испытания) — UI/UX
- **ВИ по предметам в карточке абитуриента:** сейчас ВИ хранится как единый балл `viScore`
  (0–300), заменяющий сумму ЕГЭ в `totalScore`. Нужно разобрать ВИ по предметам в форме
  абитуриента (как ЕГЭ). **Ограничение:** не перегружать интерфейс и таблицу абитуриентов —
  подобрать компактное решение (например: блок «ВИ» рядом с баллами ЕГЭ, раскрывающийся или
  переключаемый, без добавления новых колонок в таблицу).
- **Режим отображения баллов в таблице:** добавить кнопку-переключатель режима отображения.
  Первая функция переключателя — показывать колонку «Балл» раздельно: **балл ВИ** и **балл ЕГЭ**
  (вместо/помимо итогового `totalScore`). Продумать, как это согласуется с applicants, у которых
  указан только один источник баллов (только ВИ или только ЕГЭ).

### B. Безопасность — остаток аудита (см. SECURITY.md)
- **M1: rate-limit in-memory → Redis** — нужно при нескольких инстансах / горизонтальном
  масштабировании (на одном инстансе текущий in-memory работает).
- **M4: ENCRYPTION_KEY в KMS/Vault** (сейчас env).
- L1–L3, L5–L6, **nonce-CSP** (убрать 'unsafe-inline' в проде; требует dynamic rendering).
- B (на пользователе): заменить боевые JWT_SECRET/ENCRYPTION_KEY в Vercel env.

---

## Технический долг
- Апгрейд Prisma 6 → 7 (требует `prisma.config.ts` + driver adapter) — не срочно.

## Баги

### BUG. Медленный/падающий запрос `/` на проде (isua.vercel.app)
Зафиксирован сетевой анализ: `GET https://isua.vercel.app/` → `net::ERR_ABORTED`,
длительность **21.3 с** (превышает типичный лимит serverless-функции Vercel).

- **Симптом:** главный маршрут не отдаётся, запрос обрывается по таймауту платформы
  (~21 c) или ручной отмене пользователем.
- **Подозреваемое:** серверная задержка (TTFB) — корневой маршрут `/` (дашборд),
  вероятно, выполняет тяжёлый/долгий путь: cold start serverless + БД-запрос
  (статистика дашборда, `stats/daily`) или upstream-зависимость.
- **Зона расследования:**
  - Логи Vercel: таймауты выполнения функции, slow middleware, ошибки подключения к БД.
  - Лимит serverless-функции (Hobby — 10 c, Pro — 60 с по умолчанию/макс 300 с): 21 c
    намекает на упор в лимит либо внешний stall.
  - Запросы корневого маршрута и `stats/daily`: проверить индексы и время БД-запросов.
  - БД-подключение из serverless: cold connection pool / long query на дашборде.
- **Направления фикса (по итогам расследования):**
  - Сервер: явные fetch-timeout'ы и graceful handling зависших запросов.
  - БД: аудит запросов дашборда на индексы и время выполнения.
  - Кэширование/перенос тяжёлой агрегации из горячего пути корневого маршрута.

## Заметки по инфраструктуре
- ⚠️ **Локальная БД сменилась (2026-06-22):** portable PostgreSQL 17 (`%LOCALAPPDATA%\isua-pg`)
  больше нет; на :5432 — системная служба **PostgreSQL 18** (`postgresql-x64-18`,
  auth `scram-sha-256`). `DATABASE_URL` в `.env` указывает на неё (пароль роли `postgres`
  задан при установке PG18). Каталог бэкапов portable-инстанса тоже исчез — настроить
  бэкапы для PG18 (см. `.ai/ops/DATABASE_ENVIRONMENTS.md`, раздел «Бэкапы / дампы БД»).
- ПРОД/dev-preview: при следующем провижене БД создавать её с ICU ru-RU
  (см. `.ai/ops/DATABASE_ENVIRONMENTS.md`, раздел «Локаль БД: ICU ru-RU»).

> Полный реестр требований и статусы — в `REQUIREMENTS_BACKLOG.md`; безопасность — в
> `SECURITY.md`; tracking-таблица — `REQUIREMENTS_BACKLOG.md` §11.
