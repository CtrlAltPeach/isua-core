-- Итерация 17: разделение «Доп. баллы / ВИ».
-- Новое поле viScore (ВИ — вступительные испытания вуза). Если задано — заменяет
-- сумму предметов ЕГЭ в итоговом балле. Существующие additionalScores не трогаем
-- (остаются доп. баллами; новый cap 0-10 применяется только при редактировании).

-- AlterTable
ALTER TABLE "Applicant" ADD COLUMN "viScore" INTEGER;
