# REQUIREMENTS_BACKLOG: Реестр требований для ИСУА

**Все идеи и требования, собранные для будущих итераций.**

---

## 1. БД: Новые поля в модель Applicant

- [ ] `passportSeries` (string) — серия паспорта
- [ ] `passportNumber` (string) — номер паспорта
- [ ] `citizenship` (string) — гражданство (Россия, Беларусь, и т.д.)
- [ ] `documentType` (enum: diploma | certificate) — диплом или аттестат
- [ ] `specialQuota` (boolean, default: false) — особая квота
- [ ] `isPaid` (boolean, default: false) — платное обучение
- [ ] `additionalScores` (float, default: 0) — доп. баллы / ВИ (могут быть сверх 300)
- [ ] `minimumScoreThreshold` (float?) — минимальный порог экзаменов (системный или per-program?)

**Migration:** 
```bash
npx prisma migrate dev --name add_passport_citizenship_quota_paid
```

---

## 2. Таблица Applicants: UI изменения

### 2.1 Новые столбцы в таблице
- [ ] Добавить паспортные данные (серия/номер) в таблицу
- [ ] Добавить гражданство в таблицу
- [ ] Дата добавления: показывать **дата сверху, время снизу** (двухстрочный формат)
  - Пример: `09.06.2026` / `14:30:00`

### 2.2 Визуальные изменения в таблице
- [ ] Убрать кнопку **"Удалить"** из строки таблицы (см. 3.3)
- [ ] Галочки **"Согласие"** и **"Документы"**: сделать **жирнее** (bold)
  - Если `true` → ✓ (жирная)
  - Если `false` → ✗ (жирный крест, не галочка)
- [ ] После **ФИО**: добавить **букву "Б"** вместо иконки (для math_base сданный)
  - Отображается если `mathBase` заполнен
  - Пример: `Иванов И. Б | ИСД | Applied`
- [ ] Рядом с ФИО (после "Б"): добавить маркеры квот
  - **"О"** (оранжевая) если `specialQuota = true`
  - **"П"** (серая) если `isPaid = true`
  - Пример: `Иванов И. Б О П | ИСД | Applied`

### 2.4 Группировка по дням
- [ ] **Группировать строки таблицы по дате добавления** (`createdAt`)
- [ ] **Структура:**
  - Заголовок дня: `09.06.2026 (Вторник) — 12 абитуриентов` ← кликабельно (collapsible)
  - Строки абитуриентов, добавленных в этот день
  - Заголовок дня: `08.06.2026 (Понедельник) — 8 абитуриентов`
  - Строки абитуриентов
  - И т.д. в обратном хронологическом порядке (новые дни сверху)
- [ ] **Свёртывание/разворачивание:**
  - Клик на заголовок дня → свернуть/развернуть все строки этого дня
  - Иконка стрелки (↓ / ▶) для визуального указания статуса
- [ ] **Фильтры и сортировка:**
  - Группировка работает в сочетании с фильтрами (status, program, search)
  - При фильтре → остаются только абитуриенты, соответствующие условиям, но дни сохраняются
  - Сортировка по ФИО/баллам/статусу → работает внутри каждого дня
- [ ] **Пагинация:**
  - Группировка работает с пагинацией (может быть много дней)
  - Может быть: "День 1" (с абитуриентами 1-25), "День 2" и т.д.
  - Или: сначала все дни целиком (без пагинации по дням), потом пагинация по записям

### 2.3 Удаление записей
- [ ] Убрать кнопку удаления из каждой строки таблицы
- [ ] Создать отдельный интерфейс для управления удалением:
  - Новая вкладка **"Управление"** или модаль **"Удаление абитуриентов"**
  - Мультиселект (чекбоксы для выбора нескольких)
  - Кнопка **"Удалить выбранные"** с подтверждением
  - История удаления логируется

---

## 3. Карточка абитуриента (Applicant Form Modal)

### 3.1 Структура вкладок (переупорядочивание)
**Новый порядок:**
1. **Основное** (ФИО, программа, статус)
2. **Документы и согласие** ← Переместить сюда (было ниже)
   - Чекбокс "Документы собраны"
   - Чекбокс "Согласие на зачисление"
   - Чекбокс "Особая квота" ← Новое
   - Чекбокс "Платно" ← Новое
   - Выпадающий список "Документ" (диплом/аттестат)
   - Паспортные данные (серия, номер)
   - Гражданство
3. **Баллы экзаменов**
   - Математика (база) — оценка 2-5
   - Математика (профиль) — баллы 0-100
   - Русский, Химия, Физика, Информатика, География — баллы 0-100
   - **Доп. баллы / ВИ** ← Новое, 0-100+ (может быть сверх 300)
   - Средний балл (авто-расчёт)
4. **Контакты** (телефон, email)
5. **Персональное** (прописка, ИНН, СНИЛС, заметки)
6. **История** (лог изменений)

### 3.2 Логика Математики (база vs профиль)
- [ ] **Взаимоисключающие поля:**
  - Если заполнен `mathBase` (оценка) → `mathProfile` должен быть disabled (серый, не редактируется)
  - Если заполнен `mathProfile` (баллы) → `mathBase` должен быть disabled
  - Если оба пусты → оба активны
- [ ] **При расчёте total_score:**
  - Если заполнен `mathBase` → использовать ТОЛЬКО mathProfile (если есть) ИЛИ mathBase исключить
  - Если заполнен `mathProfile` → исключить mathBase (как сейчас)
  - Логика: студент сдаёт ЛИБО базу, ЛИБО профиль (не оба)

### 3.3 Валидация полей
- [ ] Убрать возможность **изменения баллов экзаменов колесиком мыши** (wheel event)
  - Разрешить только клик и ввод с клавиатуры
  - Может быть через `onWheel={(e) => e.preventDefault()}`

### 3.4 Новые поля
- [ ] **"Документ"**: выпадающий список (диплом / аттестат)
- [ ] **"Паспорт"**: серия (4 символа) и номер (6 символов)
- [ ] **"Гражданство"**: текстовое поле или выпадающий список (Россия, Беларусь, и т.д.)
- [ ] **"Особая квота"**: чекбокс (default: false)
- [ ] **"Платно"**: чекбокс (default: false)
- [ ] **"Доп. баллы / ВИ"**: числовое поле (0-100+, может быть сверх ограничения)

---

## 4. Dashboard: Новые метрики

### 4.1 Карточки метрик
**Текущие метрики:**
- Всего абитуриентов
- Новых заявлений сегодня
- Согласия на зачисление
- Документы собраны (%)

**Добавить:**
- [ ] **"Новые согласия сегодня"** — кол-во абитуриентов, у которых `consentToEnroll` изменился с false на true за сегодня
- [ ] **"Забрали согласие сегодня"** — кол-во, у которых `consentToEnroll` изменился с true на false за сегодня
- [ ] **"Заявлений / Мест"** — ratio всех заявлений к общему кол-ву мест (сумма по всем программам)
- [ ] **"Заявлений / Согласий"** — ratio зачисленных с согласием к кол-ву заявлений
- [ ] **"Платные абитуриенты"** — кол-во с `isPaid = true` и процент от всех

### 4.2 Графики и таблицы
- [ ] Все метрики выводить за **текущий день** (с учётом timezone)
- [ ] Таблица конкурса по программам: добавить столбец **"Платные"** (кол-во с `isPaid = true`)

---

## 5. Вкладка Programs: Управление программами

- [ ] Создать **новую вкладку "Программы"** (в меню: Dashboard, Абитуриенты, Программы, Статусы, Управление)
  - Или отдельная страница `/programs/manage`
- [ ] Интерфейс редактирования программ:
  - Таблица существующих программ (5 программ)
  - Колонки: Название, Кол-во мест, Действия (редактировать, удалить)
  - Модаль редактирования: ФИО программы, кол-во мест
  - Кнопка "Добавить программу"
- [ ] Все изменения логируются в History

---

## 6. Браузер и UI

### 6.1 Favicon (иконка вкладки)
- [ ] Создать новую иконку для ИСУА
- [ ] Поместить в `public/favicon.ico` (или svg)
- [ ] Обновить `layout.tsx`:
  ```jsx
  <link rel="icon" href="/favicon.ico" />
  // или для svg:
  <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
  ```

---

## 7. Валидация и ограничения

### 7.1 Минимальный порог экзаменов
- [ ] **Система минимальных баллов** за каждый предмет
  - Возможные варианты:
    1. Глобальный порог (одинаков для всех)
    2. Порог per-program (каждая программа имеет свои минимумы)
    3. Оба варианта
  - Пример: русский ≥ 36, математика ≥ 27, и т.д.
- [ ] Если балл < минимума → **вывести предупреждение** в UI (оранжевый текст или иконка)
- [ ] Не блокировать сохранение, но показывать статус "не соответствует минимуму"

### 7.2 Ограничение доп. баллов
- [ ] **Доп. баллы (ВИ)** могут быть сверх ограничения в 300 баллов
  - При расчёте total_score + additionalScores может быть > 300
  - Это нормально (отмечать как "переполнение" в UI)

---

## 8. Экспорт и отчёты

- [ ] PDF отчёт должен содержать паспортные данные
- [ ] Статистика платных абитуриентов в PDF
- [ ] При экспорте рейтинга: показывать маркеры О (квота) и П (платно)

---

## 9. История изменений

- [ ] Все новые поля должны логироваться в History:
  - `passportSeries`, `passportNumber`, `citizenship`
  - `documentType`, `specialQuota`, `isPaid`
  - `additionalScores`
- [ ] Математика (база vs профиль) — логировать переключение
- [ ] Если `consentToEnroll` меняется → отмечать в статистике дашборда

---

## 10. Приоритеты по итерациям

### Итерация 3 (NEXT — краткосрочно)
- [ ] 1. БД: добавить новые поля (паспорт, гражданство, квота, платно, доп. баллы)
- [ ] 2. Карточка абитуриента: переупорядочить вкладки, добавить новые поля
- [ ] 3. Таблица: новые столбцы, маркеры О и П, убрать кнопку удаления
- [ ] 4. Браузер: новая иконка (favicon)
- [ ] 5. Математика: взаимоисключающие базовая/профиль

### Итерация 4 (MEDIUM — среднесрочно)
- [ ] 1. Dashboard: новые метрики (согласия, платные, ratio)
- [ ] 2. Вкладка управления: удаление абитуриентов (отдельный интерфейс)
- [ ] 3. Вкладка программ: редактирование данных программ
- [ ] 4. Дата в таблице: двухстрочный формат (дата / время)
- [ ] 5. Валидация баллов: убрать wheel event, добавить минимальный порог

### Итерация 5 (FUTURE — долгосрочно)
- [ ] Минимальный порог экзаменов (система порогов)
- [ ] PDF экспорт: паспортные данные, статистика платных
- [ ] WebSocket синхронизация (real-time, фаза 5)
- [ ] Тесты и полировка

### Итерация 6 (СДЕЛАНО, см. раздел 10-ante)
- [x] Таблица: фикс-ширина ФИО, фикс-шапка, клик по строке = редактирование, распорка для «Истории»
- [x] Статусы: убран карандаш
- [x] Актуализация CONTEXT.md / NEXT_ITERATION.md

### Итерация 7 (NEXT — собрано 2026-06-13, см. раздел 10-bis)
- [ ] 7A. Дашборд: «движение согласий за день» — нетто за день, не накопление переключений
- [ ] 7B. Карточка абитуриента: вкладки → секции вертикально + скролл
- [ ] 7C. Шифрование личных данных в БД (обратимое, AES; не хэш)
- [ ] 7D. Таблица: превью заметки + пересмотр раскладки таблицы (обсудить варианты до кода)
- [ ] 7E. (дальше) Мобильный/адаптивный интерфейс — отложено
- [ ] 7F. Авто-обновление таблицы при изменениях другого пользователя (polling 15-30с)
- [ ] 7G. История: показывать название программы вместо programId

> Остаётся также из «полировки»: пересоздание БД с ICU-локалью, корректные таймзоны
> в stats/daily, toast вместо alert/confirm, тесты.

---

## 10-ante. Итерация 6 (СДЕЛАНО — задним числом, коммит 296cb4b, 2026-06-13)

Итерация возникла из устных правок (не из backlog-пунктов), поэтому раздел дописан постфактум
для совпадения нумерации с git-историей.
- [x] Таблица абитуриентов: вернули **фикс-ширину ФИО (w-72)** (после эксперимента с резиновой).
- [x] Таблица: **фикс-шапка** (sticky top-0), внутренний верт.+гориз. скролл, клик по строке = редактирование.
- [x] Таблица: колонка-распорка (w-auto) держит кнопку «История» на месте при сжатии окна.
- [x] Вкладка «Статусы»: **убрана кнопка-карандаш** (редактирование кликом по карточке).
- [x] Актуализированы PROMPT/CONTEXT.md и NEXT_ITERATION.md (убрана выдумка про shadcn/TanStack/Recharts/Bun).

---

## 10-bis. Итерация 7 (NEXT — собрано 2026-06-13)

### 7A. Дашборд: «движение согласий за день» должно ОБНОВЛЯТЬСЯ, а не накапливаться ✅ СДЕЛАНО
Реализовано: в stats/daily согласия за день считаются НЕТТО — по каждому абитуриенту
сравнивается состояние на начало дня (oldValue первого изменения) с состоянием на конец дня
(newValue последнего изменения); туда-сюда за день → 0.
- [x] **«Новые согласия сегодня»** показывают ЧИСТЫЙ итог за день (нетто), а не сумму переключений.
  - Если за день согласие забрали, а потом вернули → НЕ увеличивает «забрали».
  - Если за день согласие подали, а потом забрали → НЕ увеличивает «подали».
- [x] Считается по конечному состоянию против начального (а не сумма переключений из History).
- **Где:** `src/app/api/stats/daily/route.ts` (consentGivenToday/consentWithdrawnToday).

### 7B. Карточка абитуриента: убрать вкладки → секции одна под другой ✅ СДЕЛАНО
Реализовано: в applicant-form-modal убраны вкладки (TABS/tab/setTab), добавлен компонент
Section (заголовок-разделитель), все 5 секций идут вертикально, модал скроллится оверлеем.
- [x] Все секции (Основное, Документы, Баллы, Контакты, Персональное) идут вертикально.
- [x] История остаётся кнопкой-часами в таблице (не вкладка, не секция формы).

### 7C. Шифрование личных данных в БД (НЕ хэш — обратимое) ✅ СДЕЛАНО
Реализовано: AES-256-GCM (node:crypto) в lib/crypto.ts; формат "enc:v1:iv:tag:ct" (base64),
ключ ENCRYPTION_KEY (32 байта hex). Шифруются passportSeries, passportNumber, inn, snils
(решение пользователя — без телефона/email, поиск по ним не нужен → blind-index не понадобился).
- [x] lib/applicant-pii.ts (encryptPii/decryptPii): шифрование на границе БД.
- [x] POST/PUT/GET/список/report: пишут зашифрованным, отдают расшифрованным.
- [x] История: ПДн логируются МАСКОЙ «•••» (факт изменения без раскрытия значения).
- [x] Миграция существующих данных: prisma/encrypt-pii.ts (идемпотентно). Запущено (0 изменений
  — seed не заполняет эти поля). E2E-проверено: в БД enc:v1:…, через API открыто.
- ⚠️ ENCRYPTION_KEY в dev лежит в .env (как и JWT_SECRET) — для прода сгенерировать свой;
  при смене ключа старые данные нечитаемы.

### Безопасность (аудит 2026-06-14, вместе с 7C)
Проведён аудит auth/хранения. Исправлено сейчас:
- [x] **D:** токен убран из тела ответа login/register (остаётся только httpOnly-cookie).
- [x] **E:** rate-limit на login (lib/rate-limit.ts, in-memory, 10 попыток/15мин по IP+email → 429).
Остаточные риски (НЕ чинили — отложено, решение пользователя):
- [ ] **A:** регистрация /register полностью открыта — любой создаёт аккаунт с доступом к ПДн.
  Варианты: отключить /register, инвайт-код, или только админ создаёт юзеров.
- [ ] **B:** JWT_SECRET и ENCRYPTION_KEY — dev-значения в .env; для прода заменить на сильные.
- [ ] **C:** нет ролей (все юзеры равны) — нет разделения оператор/админ.
- Прочее на будущее: нет серверного middleware (защита маршрутов на клиенте + проверка
  getCurrentUser в каждом route — работает, но middleware надёжнее).

### 7D. Таблица: показывать кусочек заметки + редизайн ✅ СДЕЛАНО
Решение пользователя: **раскрывающаяся строка-деталь**.
- [x] Главная таблица сокращена с 13 до 9 колонок: убраны Гражданство/Паспорт/Телефон/Email
  (они тесно жались, паспорт всё равно зашифрован). Добавлена колонка-шеврон слева.
- [x] Клик по шеврону раскрывает под-строку (colSpan) с деталями: гражданство, паспорт,
  телефон, email и **заметка целиком**. Клик по строке = редактирование (stopPropagation на шевроне).
- [x] У кого есть заметка — жёлтая иконка StickyNote у ФИО (видно без раскрытия, title = текст).
- [x] min-w таблицы 58rem→44rem: на 1280px горизонтального скролла больше нет.
- Состояние раскрытия — Set<id> в expandedRows.

### 7E. (Дальние итерации) Мобильный интерфейс
- [ ] Адаптив под телефоны: навигация, таблица (карточный режим на узких экранах), формы.
- Не итерация 7 — отложено на более дальние итерации.

### 7G. История: показывать название программы, а не её ID ✅ СДЕЛАНО
Реализовано: formatHistoryValue получил опц. параметр programNames (Map id→name);
history-modal подтягивает программы и резолвит programId→название, фолбэк «Программа #id».
- [x] При изменении программы абитуриента в истории отображается **числовой programId**
  (напр. «3»), а не название программы («ИСД»). Нужно показывать название.
- **Причина:** в lib/history.ts поле `programId` логируется через toStr() как сырой id
  (oldValue/newValue = число). При показе в history-modal подмены id→название нет.
- **Варианты решения:**
  1. Резолвить id→название при ОТОБРАЖЕНИИ (history-modal): подтянуть список программ и
     маппить programId→name. Плюс: историч. записи не дублируют название; минус: если
     программу переименовали/удалили — старый id может не сматчиться.
  2. Логировать сразу название (в buildHistoryEntries для programId писать name вместо id).
     Плюс: запись самодостаточна; минус: нужно прокинуть имена программ в момент записи.
  - Рекоменд.: вариант 1 (резолв при показе) + фолбэк «Программа #<id>» если имя не найдено.
- Аналогично проверить другие FK/коды, если появятся (пока только programId).

### 7F. Авто-обновление таблицы при изменениях другого пользователя ✅ СДЕЛАНО
Реализовано: в applicant-table тихий polling — setInterval каждые 20 сек вызывает load(true)
(silent: без спиннера, без сброса строк при ошибке). load зависит от filters, поэтому
фоновый refetch уважает текущие фильтры/поиск/страницу.
- [x] Раньше таблица перечитывалась только при действиях самого пользователя; теперь видны
  изменения других клиентов без ручного refresh.
- [x] Тихий refetch: не дёргает loading-спиннер, не сбивает ввод поиска.
- [ ] Альтернативы (если polling мало): SSE (EventSource, сервер шлёт «данные изменились») или
  WebSocket-broadcast (нужен кастомный server.js — дороже).
- Связано с локами (итер. 5): локи защищают от одновременного редактирования одной записи и
  перезаписи (optimistic version → 409), но НЕ рассылают «список изменился».

---

## 11. Tracking: Что сделано

✅ = сделано  
⏳ = в работе  
❌ = не начинали  

| Требование | Статус | Итерация | Примечание |
|-----------|--------|----------|-----------|
| БД: новые поля | ✅ | 3 | migration add_passport_quota_paid_additional + re-seed |
| Карточка: переупорядочение вкладок | ✅ | 3 | 5 вкладок (без История) |
| Таблица: новые столбцы | ✅ | 3 | паспорт, гражданство, дата/время 2 строки |
| Маркеры О и П | ✅ | 3 | после ФИО: Б (math база), О (квота), П (платно) |
| **Таблица: группировка по дням** | ✅ | 4 | кнопка «По дням», свёртываемые разделители |
| Удаление: отдельный интерфейс | ✅ | 4 | вкладка «Управление», мультиселект + bulk-delete |
| Favicon | ✅ | 3 | src/app/icon.svg (emerald шапочка) |
| Математика: взаимоисключающие | ✅ | 3 | disabled в форме + обнуление на бэке; чинит баг балла |
| Dashboard: новые метрики | ✅ | 4 | согласия за день (из History), платные, ratio заявл/места и заявл/согласия |
| Вкладка программ: управление | ✅ | 4 | вкладка «Управление»: CRUD программ (POST/PUT/DELETE) |
| История изменений: модал | ✅ | 4 | кнопка-часы в таблице, человекочитаемые метки/значения |
| Минимальный порог | ✅ | 5 | per-program (minScores в Program), редактор в «Управлении», предупреждения в форме/таблице |
| Доп. баллы / ВИ | ✅ | 3 | поле в форме; total = топ-3 + ВИ (может >300) |
| Wheel на баллах | ✅ | 3 | onWheel→blur на числовых инпутах |
| BUG: Zod coerce null/""→0 | ✅ | 4 | preprocess emptyToNull; пустые баллы остаются null |
| PDF: паспортные данные | ✅ | 5 | клиентский print (/report): статистика+рейтинг с паспортом/платными/О/П |
| WebSocket-синхронизация | ✅ | 5 | REST-локи + heartbeat 10с + таймаут 30с; useLock; баннер «Редактирует …» |
| Таблица: фикс-ширина ФИО, фикс-шапка, клик по строке | ✅ | 6 | w-72, sticky, распорка для кнопки «История»; убран карандаш в Статусах |
| 7A. Согласия за день: нетто, не накопление | ✅ | 7 | stats/daily: нетто по абитуриенту (начало дня vs конец дня) |
| 7B. Карточка: секции вместо вкладок | ✅ | 7 | убраны TABS, компонент Section, 5 секций вертикально + скролл |
| 7C. Шифрование личных данных в БД | ✅ | 7 | AES-256-GCM; passport/inn/snils; история маской; миграция-скрипт |
| Безопасность D: token не в теле ответа | ✅ | 7 | только httpOnly-cookie |
| Безопасность E: rate-limit на login | ✅ | 7 | 10 попыток/15мин по IP+email → 429 |
| Безопасность A: открытая регистрация | ❌ | дальше | /register доступен всем — закрыть (инвайт/только админ) |
| Безопасность B: dev-секреты в .env | ❌ | дальше | JWT_SECRET + ENCRYPTION_KEY заменить для прода |
| Безопасность C: нет ролей | ❌ | дальше | разделение оператор/админ |
| 7D. Превью заметки в таблице + редизайн | ✅ | 7 | раскрывающаяся строка-деталь; 13→9 колонок; иконка заметки |
| 7E. Мобильный интерфейс | ❌ | дальше | адаптив, карточный режим таблицы — отложено |
| 7F. Авто-обновление таблицы (polling) | ✅ | 7 | тихий polling 20с в applicant-table (load(silent)) |
| 7G. История: название программы вместо id | ✅ | 7 | formatHistoryValue + programNames Map; фолбэк «Программа #id» |

---

## 12. Договорённости

- Все новые поля → в History логирование
- Временная зона МСК по умолчанию (для дат в таблице и дашборде)
- Зелёный акцент (emerald) для позитивных метрик
- Оранжевый для маркеров квот (О) и предупреждений
- Серый для платных (П)
- Названия файлов MD заглавными буквами
