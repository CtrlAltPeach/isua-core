# ИСУА — План следующей итерации

Обновлено: 2026-06-18. Работа в ветке `dev` (main = стабильный прод).
Закрыты итерации 1–12 (детали — CONTEXT.md, §13 бэклога). Текущая версия 0.12.0.

Итерация 12 завершена: 12A — БД пересоздана с ICU-локалью ru-RU, COLLATE-костыль убран
(поиск через Prisma `mode:"insensitive"`); 12C — retry/переподключение в fetch-обёртке,
интеграционные тесты API-роутов (55 тестов всего), README заполнен.

Тема итерации 13 не выбрана — ниже кандидаты из остаточного бэклога.

---

## Кандидаты на итерацию 13 (выбрать тему)

### B. Безопасность — остаток аудита (§13)
Самое весомое из оставшегося:
- **M3: ротация ключа шифрования** (формат `enc:v2` + чтение двумя ключами) — снимает боль
  «сменил ENCRYPTION_KEY → данные нечитаемы». Практически полезно.
- **M1: rate-limit in-memory → Redis** — нужно при нескольких инстансах / горизонтальном
  масштабировании (на одном инстансе текущий in-memory работает).
- **M4: ENCRYPTION_KEY в KMS/Vault** (сейчас env).
- L1–L3, L5–L6, **nonce-CSP** (убрать 'unsafe-inline' в проде; требует dynamic rendering).
- B (на пользователе): заменить боевые JWT_SECRET/ENCRYPTION_KEY в Vercel env.

### D. Функциональные доработки (если важнее, чем безопасность)
- Что-то из продуктовых хотелок (уточнить у пользователя) — новые поля/отчёты/фильтры,
  экспорт, печать списков, и т.п. Бэклог продуктовых требований — в §1–§9.

---

## Технический долг
- Апгрейд Prisma 6 → 7 (требует `prisma.config.ts` + driver adapter) — не срочно.

## Заметки по инфраструктуре (12A)
- Локальная C-locale БД сохранена как `isua_c_backup` (откат через обратный RENAME);
  когда новая ICU-БД подтверждена в работе — backup можно удалить.
- ПРОД/dev-preview: при следующем провижене БД создавать её с ICU ru-RU
  (см. PROMPT/DATABASE_ENVIRONMENTS.md, раздел «Локаль БД: ICU ru-RU»).

> Полный реестр требований и статусы — в REQUIREMENTS_BACKLOG.md (§13 — безопасность,
> tracking-таблица — §11).
