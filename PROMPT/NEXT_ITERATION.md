# ИСУА — План следующей итерации

Обновлено: 2026-06-17 (после итерации 9). Опирается на PROMPT/ и накопленные правки.
Работа ведётся в ветке `dev` (main = стабильный прод).

> Итерации 1–9 закрыты. Итерация 9 (полировка+инфра): unit-тесты (vitest, 36),
> корректные таймзоны (Intl), toast вместо alert/confirm, RHF watch()→useWatch,
> документация сред БД (preview-БД для dev). Версия 0.9.0. Ранее: итер.8 — роли/
> безопасность (A/C/K1/K2); 0.8.1 — Особое право (ОП/ОК-маркеры). Детали — в CONTEXT.md.

---

## Остаточный бэклог

### Приоритет 0 — Безопасность (остаток аудита §13)
Закрыто в итер. 8: A (регистрация), C+K1 (роли/доступ), K2 (rate-limit register/enumeration).
Осталось из аудита:
- **H1:** обход rate-limit через подделку `X-Forwarded-For` — доверять XFF только за известным
  reverse-proxy (на Vercel XFF проставляет их прокси — учесть при настройке trust).
- **H2:** JWT нельзя отозвать; logout не инвалидирует токен (24ч). Фикс: `tokenVersion` в User.
- **H3:** нет security-заголовков/CSP (`next.config.ts` пуст) — добавить headers().
- **B:** заменить боевые `JWT_SECRET`/`ENCRYPTION_KEY` (значения — на пользователе; задокументировано).
- M1–M5, L1–L6 — см. §13 бэклога (rate-limit in-memory, ротация ключа, и т.д.).

### Приоритет 1 — Инфраструктура БД
**Пересоздать кластер с ICU/русской локалью.**
Сейчас `--locale=C`, поэтому нативный `ILIKE`/сортировка не учитывают регистр кириллицы.
Обойдено костылём `lower(... COLLATE "und-x-icu")` в raw SQL поиска.

Что сделать:
- `initdb` заново с ICU-провайдером и локалью `ru-RU` (или `und-x-icu`).
- Убрать ICU-COLLATE из raw-запроса → вернуть Prisma `mode: "insensitive"`.
- Пере-seed данных.

### Приоритет 2 — Полировка
- ✅ Временные зоны (lib/timezone.ts, итер.9)
- ✅ Toast вместо alert/confirm (итер.9)
- ✅ Тесты unit (vitest, 36, итер.9)
- **Retry/переподключение** при потере соединения — осталось.
- **Интеграционные тесты** (API-роуты) — осталось.
- **Заполнить README**

### Приоритет 3 — Мобильный интерфейс (7E, отложено)
- Адаптив под телефоны: навигация, таблица (карточный режим на узких экранах), формы.

---

## Технический долг (мелкое)
- RHF `watch()` в форме даёт ESLint-warning `incompatible-library` (React Compiler не
  мемоизирует компонент). Не критично; при желании — `useWatch`/контролируемые поля.
- Апгрейд Prisma 6 → 7 (требует `prisma.config.ts` + driver adapter) — не срочно.
