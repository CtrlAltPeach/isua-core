# ИСУА — План следующей итерации

Обновлено: 2026-06-13 (после итерации 6). Опирается на PROMPT/ и накопленные правки.

> Итерации 1–6 закрыты. Сделано: backend+авторизация, дашборд, таблица абитуриентов,
> расширенные поля + вкладки карточки, история-модал, метрики, группировка по дням,
> минимальный порог баллов, PDF-отчёт, блокировки записей, вкладки «Программы» и «Статусы»,
> управление (/manage), полировка таблицы (фикс-ширина ФИО, фикс-шапка, клик по строке).
> Детали — в CONTEXT.md, раздел «Что уже сделано».

---

## Остаточный бэклог

### Приоритет 1 — Инфраструктура БД
**Пересоздать кластер с ICU/русской локалью.**
Сейчас `--locale=C`, поэтому нативный `ILIKE`/сортировка не учитывают регистр кириллицы.
Обойдено костылём `lower(... COLLATE "und-x-icu")` в raw SQL поиска.

Что сделать:
- `initdb` заново с ICU-провайдером и локалью `ru-RU` (или `und-x-icu`).
- Убрать ICU-COLLATE из raw-запроса → вернуть Prisma `mode: "insensitive"`.
- Пере-seed данных.

### Приоритет 2 — Полировка
- **Временные зоны:** `stats/daily` упрощён (МСК=UTC+3, прочие=UTC). Сделать корректный
  расчёт границ суток для любой зоны (date-fns-tz / Intl).
- **Toast-уведомления** вместо `alert/confirm` (удаление, ошибки).
- **Retry/переподключение** при потере соединения.
- **Тесты:** unit (calculateTotalScore, normalizeConsent, history) + интеграционные.
- **Заполнить README**

---

## Технический долг (мелкое)
- RHF `watch()` в форме даёт ESLint-warning `incompatible-library` (React Compiler не
  мемоизирует компонент). Не критично; при желании — `useWatch`/контролируемые поля.
- Апгрейд Prisma 6 → 7 (требует `prisma.config.ts` + driver adapter) — не срочно.
